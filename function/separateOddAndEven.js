const separateOddAndEven = (arr) => {
  let odd = [];
  let even = [];
  for (var t in arr) {
    if (arr[i] % 2 !== 0) {
      odd.push(arr[i]);
    } else {
      even.push(arr[i]);
    }
  }
  console.log(odd);
  console.log(even);
};
